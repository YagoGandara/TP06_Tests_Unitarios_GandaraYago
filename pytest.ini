[pytest]
asyncio_mode = auto
# Evitamos que deprecations externas rompan la corrida
filterwarnings =
    ignore:The 'app' shortcut is now deprecated:DeprecationWarning:httpx\..
# Si antes tenías -W error en addopts, quítalo o déjalo sólo para RuntimeWarning si querés:
# addopts = -q
